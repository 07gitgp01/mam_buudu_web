// En dev : le proxy Angular (proxy.conf.json) transfère /api → localhost:3000
// En production : remplacer par l'URL réelle du serveur
export const API_BASE_URL = 'https://mam-buudu-api.onrender.com';
